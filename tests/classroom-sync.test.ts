import { describe, expect, it } from "vitest";
import { syncClassroom } from "../src/classroom/sync.js";
describe("Classroom sync", () => { it("persists only published work from active courses", async () => { const saved: unknown[] = []; const client = { listCourses: async () => [{ id: "active", name: "A", courseState: "ACTIVE" }, { id: "archived", name: "B", courseState: "ARCHIVED" }], listCourseWork: async () => [{ id: "one", courseId: "active", title: "Essay", state: "PUBLISHED", dueDate: { year: 2026, month: 10, day: 1 }, dueTime: { hours: 9 } }, { id: "draft", courseId: "active", title: "Draft", state: "DRAFT" }] }; const repo = { upsertCoursework: async (items: unknown[]) => { saved.push(...items); } }; await expect(syncClassroom("u", client, repo)).resolves.toEqual({ courses: 2, coursework: 1 }); expect(saved[0]).toMatchObject({ courseId: "active", courseworkId: "one", dueAt: "2026-10-01T09:59:59.000Z" }); }); });
